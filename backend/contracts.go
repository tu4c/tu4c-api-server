package backend

// managing interaction to ethereum contract
type Contract struct {
}

func (c *Contract) EnrollInfo(user string) (*types.EnrollInfo, error) {
	// subject, enrollInfo :=
	// EnrollInfo = enrollInfo
	// ENrollInfo.SetSubject(subject)
	return nil, nil
}
